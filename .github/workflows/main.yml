name: Generate Stats

on:
  push:
    branches: [ main ]
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: stats
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        architecture: 'x64'
        cache: 'pip'

    - name: Install requirements
      run: |
        python3 -m pip install --upgrade pip setuptools wheel
        python3 -m pip install -r requirements.txt

    - name: Run tests
      run: python3 -m pytest -q

    - name: Generate stats, SVGs and page values
      run: |
        python3 --version
        python3 generate_images.py
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        EXCLUDE_CONTRIBUTED_REPOS: false
        EXCLUDED: boredland/boredland

    - name: Commit to the repo
      run: |
        git config --global user.name "github-stats"
        git config --global user.email "github-stats[bot]@users.noreply.github.com"
        git add .
        git commit -m 'Update generated stats' || true
        git pull --rebase --autostash
        git push
